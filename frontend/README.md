# Frontend

Real-time dashboard skeleton.

Suggested responsibilities:

- Connect to backend WebSocket
- Show latest robot telemetry packet
- Show metric cards
- Show charts for latency, stability, base pose and joint states
- Show event timeline
- Add replay controls

Suggested folders:

```text
src/
├── api/
├── websocket/
├── store/
├── components/
├── pages/
└── types/
```
